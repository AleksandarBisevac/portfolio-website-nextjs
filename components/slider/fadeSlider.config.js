export default function getSettings(dots, slidesToShow) {
  return {
    fade: true,
    infinite: true,
    autoplay: true,
    autoplaySpeed: 2500,
    speed: 500,
    slidesToShow: 1,
    cssEase: 'linear',
  };
}
